{
  "name": "chinese-learning-studio",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node ./scripts/build.mjs",
    "serve:dist": "python3 -m http.server 4173 -d dist"
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "dependencies": {
    "@tykok/cedict-dictionary": "^2.7.14"
  }
}
